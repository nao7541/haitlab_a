export default {
    myUserData(state) {
        return state.myUserData;
    },
    myTags(state) {
        return state.myTags;
    },
    myEventStocks(state) {
        return state.myEventStocks;
    }
};