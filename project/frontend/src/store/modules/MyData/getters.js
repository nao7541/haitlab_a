export default {
    myUserData(state) {
        return state.myUserData;
    },
    myTags(state) {
        return state.myTags;
    },
    myEventIDs(state) {
        return state.myEventIDs;
    }
};